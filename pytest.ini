[pytest]
# Les tests vivent sous tests/ — le script manuel test_ollama.py (racine) est
# délibérément hors collecte.
testpaths = tests
# Rend les modules de la racine (config, claude_analyzer, ...) importables.
pythonpath = .
addopts = -q
